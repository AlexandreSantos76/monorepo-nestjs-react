export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL;

// Observação:
// Ajuste o valor acima conforme seu backend (ex.: "https://api.seudominio.com").
// Em desenvolvimento você pode usar um proxy no Vite para apontar "/api" para seu backend.
